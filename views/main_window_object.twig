{% extends "__layout.twig" %}
{% import "_macros.twig" as macros %}

{% block content %}
<h1>{{ title_objects.title }}</h1>
<div>{{ description }}</div>
<div class="container">
        <ul class="nav nav-pills">
        {% for item in newnav %}
            {{ macros.nav(item.title, "/space-object/" ~ title_objects.id ~ "/" ~ item.url, temp == item.title) }}
        {% endfor %}
        </ul>
</div>

<div style="margin-top: 15px;">
        <ul class="list-group">
                <li class="list-group-item">
                    {% block dopcontent %}
                        Выберите вверху информацию, которую хотите увидеть<br>
                    {% endblock %}
                </li>
        </ul>
</div>
{% endblock %}